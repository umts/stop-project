.page-title Route #{@route.number} Bus Stops

%div
  = link_to 'View by status', by_status_bus_stops_path(number: @route.number)
- if @stops.empty?
  No stops on this route
- else
  - @collection.each do |direction, bsrs|
    = direction
    - bsrs.each do |bus_stop_route|
      .row
        .col-sm-7
          = link_to edit_bus_stop_path(bus_stop_route.bus_stop.hastus_id, route_id: @route.id, direction: direction) do
            = bus_stop_route.bus_stop.name
        .col-sm-1
          = bus_stop_route.bus_stop.hastus_id
