.page-title Route #{@route.number} Bus Stops

- if @stops.empty?
  No stops on this route
- else
  - @stops_arr.each_pair do |status, stops|
    - case status
    - when :pending
      Pending
    - when :not_started
      Not Started
    - when :completed
      Completed
    Stops
    %ul
    - stops.each do |stop|
      %li
        = link_to edit_bus_stop_path(stop.hastus_id) do
          - stop.name
