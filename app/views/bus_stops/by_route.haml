.page-title Route #{@route.number} Bus Stops

- if @stops.empty?
  No stops on this route
- else

  Incomplete Stops
  %ul
    - @stops.where(completed: false).each do |stop|
      %li
        = link_to edit_bus_stop_path(stop.hastus_id) do
          - stop.name
  Completed Stops
  %ul
    - @stops.where(completed: true).each do |stop|
      %li
        = link_to edit_bus_stop_path(stop.hastus_id) do
          - stop.name

